{
  "title": "Company Registry & Filings Aggregator input",
  "description": "Look up official entity-level company registry or SEC filing records for AI company research, due diligence briefs, and firmographic enrichment.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "sources": {
      "title": "Sources",
      "type": "array",
      "description": "Official APIs to query. Use SEC EDGAR for no-key US company and filing lookups. Use UK Companies House only when you provide a Companies House API key.",
      "editor": "select",
      "default": ["sec_edgar"],
      "items": {
        "type": "string",
        "enum": ["companies_house", "sec_edgar"],
        "enumTitles": ["UK Companies House", "US SEC EDGAR"]
      },
      "uniqueItems": true
    },
    "query": {
      "title": "Search query",
      "type": "string",
      "description": "Company name, ticker, or broad search term. Agents should use a specific company name or ticker when possible.",
      "editor": "textfield",
      "default": "Microsoft"
    },
    "companyNames": {
      "title": "Company names",
      "type": "array",
      "description": "Optional list of company names or tickers to search for batch company research.",
      "editor": "stringList",
      "items": {
        "type": "string"
      }
    },
    "companyNumbers": {
      "title": "Companies House numbers",
      "type": "array",
      "description": "Optional direct UK Companies House company numbers for precise entity lookup.",
      "editor": "stringList",
      "items": {
        "type": "string"
      }
    },
    "ciks": {
      "title": "SEC CIKs",
      "type": "array",
      "description": "Optional direct SEC CIK identifiers for precise SEC entity lookup.",
      "editor": "stringList",
      "items": {
        "type": "string"
      }
    },
    "maxResults": {
      "title": "Maximum records",
      "type": "integer",
      "description": "Maximum clean company/entity records to save. Use 1-10 for agent calls and focused due diligence.",
      "editor": "number",
      "default": 10,
      "minimum": 1,
      "maximum": 1000
    },
    "companiesHouseApiKey": {
      "title": "Companies House API key",
      "type": "string",
      "description": "Free Companies House API key. Required only when UK Companies House is selected. You may also set COMPANIES_HOUSE_API_KEY.",
      "editor": "textfield",
      "isSecret": true
    },
    "secUserAgent": {
      "title": "SEC User-Agent",
      "type": "string",
      "description": "SEC requires a descriptive User-Agent. Replace contact@example.com with your contact email for production use.",
      "editor": "textfield",
      "default": "CompanyRegistryFilingsAggregator/1.0 contact@example.com"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Not required for official JSON APIs, included for Apify compatibility.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": false
      }
    }
  },
  "required": []
}
